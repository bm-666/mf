upstream fe {
    server fe:3000;
}
server {
    listen 80;
    server_name localhost;
    location / {
        try_files $uri $uri/ @fe;
        
    }
    location @fe {
        proxy_pass http://fe;
    }
        
}
